vim.cmd.colorscheme('everforest')
-- local status, _ = pcall(vim.cmd.colorscheme('seoul256'))
-- if not status then
--    print("Colorscheme not found! You probably haven't installed it.")
--    return
-- end

